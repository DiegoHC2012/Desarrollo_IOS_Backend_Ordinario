export const mockAnnouncementsRepository = {
  getAnnouncements: jest.fn(),
  getAnnouncement: jest.fn(),
  createAnnouncement: jest.fn(),
  updateAnnouncement: jest.fn(),
  deleteAnnouncement: jest.fn()
};
